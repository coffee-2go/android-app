<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.coffee2go">

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Coffee2go">
        <activity
            android:name=".view.activities.MenuItemActivity"
            android:exported="false" />
        <activity
            android:name=".view.activities.MainActivity"
            android:exported="false" />
        <activity
            android:name=".view.activities.CategoryItemsActivity"
            android:exported="false" />
        <activity
            android:name=".view.activities.MenuActivity"
            android:exported="false" />
        <activity
            android:name=".view.activities.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".view.activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>